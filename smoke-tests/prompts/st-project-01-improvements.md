Implement the highest-value improvements for the project in this directory based
on the prior assessment session.

Use `./session-artifacts/assessment.md` as the source of truth for the prior
assessment findings and priorities.

Goals:
- address structural/code quality issues, not just polish
- improve determinism and edge-case handling where needed
- strengthen tests and README where appropriate
- preserve the bounded CLI shape and avoid scope creep

Required finish:
- implement the chosen improvements
- run local verification
- stop at a clean, meaningful improvement point rather than continuing
  indefinitely
